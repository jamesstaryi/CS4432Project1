James Yi - 663227861

Section I:
    javac main.java
    java main "# of frames"
        e.g "java main 3"
    List of commands afterwards (Have to be capitalized):
        GET "k"
        SET "k" "string of 40 bytes" (string has quotes around them)
        PIN "BID"
        UNPIN "BID"
        QUIT (To exit the program)
            e.g SET 430 "F05-Rec450, Jane Do, 10 Hill Rd, age020."

Section II:
    Using the example test cases, it runs through perfectly besides the very last "PIN 6" where its evicting file 1 from frame 2 instead of file 2 from frame 3.
